#include <stdio.h> // แก้ไข: เพิ่ม '#' และ '<>'

int main() {
    int a = 10; // แก้ไข: เพิ่ม ';'
    printf("a = %d\n", a); // แก้ไข: เพิ่ม ';' และเพิ่ม '\n' เพื่อให้ขึ้นบรรทัดใหม่หลังแสดงผล
    return 0; // แก้ไข: เพิ่ม ';'
}
